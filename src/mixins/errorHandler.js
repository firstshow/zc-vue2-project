/**
 * ajax 错误统一处理
 * */

export default {
    data() {
        return {

        }
    },
    methods: {
        /**
         * 错误统一处理的方法
         * */
        errorHandler(res){
            if(res.code == 209) {

            }else{

            }
        }
    }
}
